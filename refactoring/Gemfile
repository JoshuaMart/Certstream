# frozen_string_literal: true

source 'https://rubygems.org'

ruby '>= 4.0.0'

# HTTP client
gem 'httpx'

# WebSocket client (async ecosystem)
gem 'async'
gem 'async-websocket'

# CLI
gem 'thor'

# Console colors
gem 'colorize'

group :development do
  gem 'rubocop', require: false
end
