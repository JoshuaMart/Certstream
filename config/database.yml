# Database configuration
host: "mysql"
database: "certstream"
username: "certstream"
password: "certstream"

# Connection pool settings
pool: 5
timeout: 5000

# Performance settings
encoding: "utf8mb4"
reconnect: true
